[Unit]
Description=Notify on Auth Service
After=network.target

[Service]
ExecStart=/usr/bin/notify -f /etc/journalCheck/config.json
ExecStop=/bin/kill -INT $MAINPID

[Install]
WantedBy=multi-user.target